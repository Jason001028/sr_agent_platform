# 测试数据 / 依赖（不入库）；脚本本身入库 —— 2026-09-15 前整个 .e2e/ 被忽略，
# test-scenes.js（45 断言的浏览器回归）就是这么丢的，只能按当前实现重写。
# 可复跑的回归是资产，不是缓存，所以只忽略依赖与大图，不忽略 .js。
.e2e/node_modules/
.e2e/fixtures/
.e2e/*.tif
.e2e/*.jpg

# 压缩包
*.zip
*.tar.gz

# 离线交付打包产物
release/

# 实施计划（内部参考，不入库）
docs/planning/web-plan.md

# Python
__pycache__/
*.pyc

# 运行时 SQLite（agent 会话/message/任务表）
sr_agent.db
sr_agent.db-journal

#参考书
docs/agent-book

#遥感数据
test-tifs

#参考源码
langchain-master
langgraph-main
deepseek-harness-master
CloudReview_Update_20260904_ratio_queue_excel_v1_6_15