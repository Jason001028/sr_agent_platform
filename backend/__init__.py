"""sr_agent_platform backend package.

Sub-packages follow docs/conventions/naming-conventions.md §4:

  mta_grid/  — pure algorithms (numpy-only, no I/O or framework deps)
  tools/     — agent-callable tools (thin JSON-serializable wrappers)
  api/       — REST routes (later)
  services/  — business logic (later)
  agent/     — orchestration (later)
"""
